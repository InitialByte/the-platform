{
  "name": "@the_platform/smoke",
  "description": "Smoke testing",
  "version": "1.0.0",
  "dependencies": {
    "cypress": "^6.2.1"
  },
  "scripts": {
    "start": "cypress run --config-file cy.json --spec 'test/*' --headless --browser chrome"
  }
}
