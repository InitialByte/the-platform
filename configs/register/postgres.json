{
  "Node": "${POSTGRES_NAME}",
  "Address": "${POSTGRES_NAME}:${POSTGRES_PORT}",
  "Service": {
    "ID": "${POSTGRES_NAME}1",
    "Service": "${POSTGRES_NAME}",
    "Port": ${POSTGRES_PORT}
  },
  "Checks": [
    {
      "Name": "http-check",
      "status": "passing",
      "Definition": {
        "http": "http://${POSTGRES_NAME}:${POSTGRES_PORT}",
        "interval": "30s"
      }
    }
  ]
}
